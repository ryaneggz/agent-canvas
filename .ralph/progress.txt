# Ralph Progress Log
Started: Sun Mar 15 23:25:31 MDT 2026

## Codebase Patterns
- Port 5173 may be in use by another app; Vite will auto-increment to 5174/5175
- Component stubs must accept props matching Canvas's usage — update interfaces when wiring up
- App.tsx renders `<Canvas />` aliased as `<AgentCanvas />`; Canvas uses the hook directly
- Typecheck/lint/test commands: `npm run typecheck`, `npm run lint`, `npm test` (run from app/)
- Inline styles only — all colors via `T` token object from `theme.ts`, no CSS variables
- The `useCanvasInteractions` hook returns all state and actions — components are stateless presentational layers
- Hook uses `useEffect` with window-level mousemove/mouseup for drag/resize/pan — deps must include dragging, resizing, panning, zoom, canvasOffset
- CSS classes (.toolbar-btn, .toolbar-btn.primary, .spawner-option) go in global.css with hardcoded hex values matching T tokens
---

## 2026-03-15 23:26 - US-001
- Verified project scaffold is complete with all prescribed files
- Confirmed Vite boilerplate removed (no App.css, index.css, assets/)
- All 16 source files present in correct directory structure
- Only react/react-dom as runtime dependencies
- Typecheck, lint, and tests all pass
- Browser verified: app renders "Agent Canvas" at localhost
- Files changed: none (scaffold was already committed)
- **Learnings for future iterations:**
  - The scaffold commit already created all stub files — US-001 was pre-done
  - Port 5173 is occupied by another app on this machine; use a different port for testing
  - The app's working directory for npm commands is `app/`, not project root
---

## 2026-03-15 23:28 - US-002
- Added Google Fonts @import for DM Sans and JetBrains Mono (400/500/600/700) with display=swap
- Added `import './styles/global.css'` to main.tsx
- Files changed: src/styles/global.css, src/main.tsx
- **Learnings for future iterations:**
  - global.css must be imported in main.tsx — it was not imported by the scaffold
  - Font @import must be at the very top of global.css before any other rules
  - DM Sans for UI chrome, JetBrains Mono for terminal content (applied via inline styles on individual components)
---

## 2026-03-15 23:29 - US-003
- Implemented T token object in theme.ts with all 19 color keys matching spec exactly
- All values use `as const` for type safety
- Files changed: src/theme.ts
- **Learnings for future iterations:**
  - The spec says "20 keys" but actually lists 19 — follow the actual key list, not the count
  - All colors reference T object directly via inline styles — no CSS variables allowed
---

## 2026-03-15 23:34 - US-004
- Implemented all TypeScript interfaces and type aliases in types.ts
- Exports: LineType, SessionLine, SessionStatus, ShellType, Session, PanelState, DragState, ResizeState, PanState, SpawnerTemplate, ShellBadgeStyle
- All types match spec exactly (SessionLine uses {t, v} shorthand)
- Files changed: src/types.ts
- **Learnings for future iterations:**
  - types.ts was a stub (`export {}`) from the scaffold — replaced entirely
  - Previous iteration left uncommitted changes in working tree; prd.json was also reverted — always verify committed vs working state
  - All type definitions are in a single file for easy import across modules
---

## 2026-03-15 23:36 - US-005
- Implemented `clamp()` and `uid()` in helpers.ts matching spec exactly
- Implemented `lineColor()`, `lineIcon()`, `statusColor()`, `shellBadge()` in lineFormatting.ts
- All return values match spec tables exactly
- Files changed: src/utils/helpers.ts, src/utils/lineFormatting.ts
- **Learnings for future iterations:**
  - Utility stubs were `export {}` — replaced entirely
  - lineFormatting.ts imports both `T` from theme.ts and types from types.ts
  - shellBadge bg values use raw rgba strings, not T token references (spec-specific values)
  - Switch statements on union types provide exhaustiveness checking without a default case
---

## 2026-03-15 23:40 - US-006
- Implemented `MOCK_SESSIONS` (4 sessions) and `SPAWNER_TEMPLATES` (4 templates) in mockSessions.ts
- api-server: bash/active/8 lines, git-workflow: zsh/active/8 lines, docker-build: bash/idle/6 lines, test-runner: bash/error/5 lines
- All line types, statuses, shell types, and content match spec exactly
- Files changed: src/data/mockSessions.ts
- **Learnings for future iterations:**
  - mockSessions.ts was a stub (`export {}`) — replaced entirely
  - Multi-line stdout in the spec (e.g., docker build output) should be combined into a single SessionLine with `\n` to match line counts
  - The "On mount, 4 panels" criterion belongs to the hook (US-007), not this data story
  - Mock data imports types from `../types` — keep type imports consistent
---

## 2026-03-15 23:40 - US-007
- Implemented full `useCanvasInteractions` hook in hooks/useCanvasInteractions.ts
- State: panels, activeId, dragging, resizing, panning, canvasOffset, zoom, showSpawner, maxZ ref
- Actions: bringToFront, closePanel, tilePanels, spawnSession, resetView
- Handlers: startDrag, startResize, startPan, handleWheel, setShowSpawner
- Initializes 4 panels from MOCK_SESSIONS with cascading positions (x=40+i*60, y=40+i*50)
- Window-level mousemove/mouseup in useEffect with proper cleanup
- Files changed: src/hooks/useCanvasInteractions.ts
- **Learnings for future iterations:**
  - The hook stub was `export {}` — replaced entirely
  - Hook contains both state management (US-007) and interaction logic (US-008/US-009) since they're all in one file
  - `startDrag` reads panels inside `setPanels` to get current panel position for offset calc (avoids stale closure)
  - `tilePanels` uses canvasOffset to position panels relative to current view, not absolute
  - React.MouseEvent types for handler params, native MouseEvent for window listeners
---

## 2026-03-15 23:44 - US-008
- Wired up Canvas.tsx with useCanvasInteractions hook — 3-level transform nesting, dot grid, pointer events
- Implemented SessionPanel.tsx with drag (title bar), resize (corner handle), close (red dot), bringToFront
- Updated Toolbar, SpawnerModal, KeyboardHints stubs to accept props from Canvas
- Updated App.tsx to render Canvas component (aliased as AgentCanvas)
- Updated App.test.tsx to check for 'api-server' panel text instead of removed 'Agent Canvas' div
- Cursor shows 'grabbing' during canvas pan
- Files changed: App.tsx, App.test.tsx, Canvas.tsx, SessionPanel.tsx, Toolbar.tsx, SpawnerModal.tsx, KeyboardHints.tsx
- **Learnings for future iterations:**
  - When Canvas wires up child components, their stubs must accept the props Canvas passes — use `void` for unused params
  - SessionPanel props: panel, isActive, onDragStart, onResizeStart, onClose, onBringToFront
  - Toolbar props: panelCount, zoom, onNewSession, onTile, onResetView
  - SpawnerModal props: onSpawn, onClose
  - The smoke test checks for rendered panel content — update if rendering changes
  - Terminal body and full line rendering are placeholders — full implementation in US-012/US-013
---

## 2026-03-15 23:47 - US-009
- Verified all 5 panel actions already implemented in useCanvasInteractions hook (from US-007/US-008)
- bringToFront: increments maxZ ref, updates panel z, sets activeId — wired to SessionPanel onBringToFront
- closePanel: filters panel array, clears activeId if match — wired to red traffic light onClick
- tilePanels: grid layout with cols=ceil(sqrt(n)), tileW=500, tileH=360, gap=20 — wired to Toolbar onTile
- spawnSession: creates panel with uid(), random position offset by canvasOffset, 2 initial lines, closes spawner — wired to SpawnerModal onSpawn
- resetView: sets canvasOffset to (0,0) and zoom to 1 — wired to Toolbar onResetView
- All quality checks pass: typecheck, lint, test
- Files changed: .ralph/prd.json (mark US-009 passes: true)
- **Learnings for future iterations:**
  - US-009 actions were already fully implemented in the hook during US-007 — this story was essentially a verification pass
  - All actions are wired through Canvas.tsx to child components via props
  - Toolbar and SpawnerModal are still stubs — they accept props but render empty divs until their own stories (US-011, US-015)
---

## 2026-03-15 23:50 - US-010
- Verified Canvas.tsx already implements all acceptance criteria (done during US-008 wiring)
- Canvas fills viewport (100vw x 100vh, overflow: hidden, background: T.bg)
- Dot grid uses radial-gradient with T.gridDot, spacing 24*zoom px, moves with pan/zoom
- 3-level transform nesting: canvas-area → zoom-layer (scale) → pan-layer (translate) → panels
- zoom-layer and pan-layer carry data-canvas="true" for click-through
- All quality checks pass: typecheck, lint, test
- Files changed: .ralph/prd.json (mark US-010 passes: true)
- **Learnings for future iterations:**
  - US-010 was already fully implemented during US-008 — Canvas.tsx was built complete when wiring up pointer interactions
  - The canvas-area div receives both mouseDown (pan) and onWheel (zoom) events
  - Dot grid backgroundPosition uses canvasOffset * zoom to track with panning
---

## 2026-03-15 23:52 - US-011
- Implemented full Toolbar component with logo, divider, 3 buttons, spacer, and status text
- Added .toolbar-btn and .toolbar-btn.primary CSS classes to global.css
- Logo: 26x26 gradient square with >_ character + "Agent Canvas" label
- Status text: "{N} session(s) · {zoom}%" with JetBrains Mono font
- All buttons wired to props: onNewSession, onTile, onResetView
- Files changed: src/components/Toolbar.tsx, src/styles/global.css
- **Learnings for future iterations:**
  - toolbar-btn CSS classes use hardcoded hex values matching T tokens (CSS can't reference JS const)
  - The primary button variant uses rgba for background/border, not T tokens
  - global.css will accumulate more classes in US-017 (scrollbar, animations, spawner-option)
---
