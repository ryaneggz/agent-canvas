# Ralph Progress Log
Started: Sun Mar 15 23:25:31 MDT 2026

## Codebase Patterns
- Port 5173 may be in use by another app; Vite will auto-increment to 5174/5175
- Component stubs must accept props matching Canvas's usage — update interfaces when wiring up
- App.tsx renders `<Canvas />` aliased as `<AgentCanvas />`; Canvas uses the hook directly
- Typecheck/lint/test commands: `npm run typecheck`, `npm run lint`, `npm test` (run from app/)
- Inline styles only — all colors via `T` token object from `theme.ts`, no CSS variables
- The `useCanvasInteractions` hook returns all state and actions — components are stateless presentational layers
- Hook uses `useEffect` with window-level mousemove/mouseup for drag/resize/pan — deps must include dragging, resizing, panning, zoom, canvasOffset
- CSS classes (.toolbar-btn, .toolbar-btn.primary, .spawner-option) go in global.css with hardcoded hex values matching T tokens
---

## 2026-03-15 23:26 - US-001
- Verified project scaffold is complete with all prescribed files
- Confirmed Vite boilerplate removed (no App.css, index.css, assets/)
- All 16 source files present in correct directory structure
- Only react/react-dom as runtime dependencies
- Typecheck, lint, and tests all pass
- Browser verified: app renders "Agent Canvas" at localhost
- Files changed: none (scaffold was already committed)
- **Learnings for future iterations:**
  - The scaffold commit already created all stub files — US-001 was pre-done
  - Port 5173 is occupied by another app on this machine; use a different port for testing
  - The app's working directory for npm commands is `app/`, not project root
---

## 2026-03-15 23:28 - US-002
- Added Google Fonts @import for DM Sans and JetBrains Mono (400/500/600/700) with display=swap
- Added `import './styles/global.css'` to main.tsx
- Files changed: src/styles/global.css, src/main.tsx
- **Learnings for future iterations:**
  - global.css must be imported in main.tsx — it was not imported by the scaffold
  - Font @import must be at the very top of global.css before any other rules
  - DM Sans for UI chrome, JetBrains Mono for terminal content (applied via inline styles on individual components)
---

## 2026-03-15 23:29 - US-003
- Implemented T token object in theme.ts with all 19 color keys matching spec exactly
- All values use `as const` for type safety
- Files changed: src/theme.ts
- **Learnings for future iterations:**
  - The spec says "20 keys" but actually lists 19 — follow the actual key list, not the count
  - All colors reference T object directly via inline styles — no CSS variables allowed
---

## 2026-03-15 23:34 - US-004
- Implemented all TypeScript interfaces and type aliases in types.ts
- Exports: LineType, SessionLine, SessionStatus, ShellType, Session, PanelState, DragState, ResizeState, PanState, SpawnerTemplate, ShellBadgeStyle
- All types match spec exactly (SessionLine uses {t, v} shorthand)
- Files changed: src/types.ts
- **Learnings for future iterations:**
  - types.ts was a stub (`export {}`) from the scaffold — replaced entirely
  - Previous iteration left uncommitted changes in working tree; prd.json was also reverted — always verify committed vs working state
  - All type definitions are in a single file for easy import across modules
---

## 2026-03-15 23:36 - US-005
- Implemented `clamp()` and `uid()` in helpers.ts matching spec exactly
- Implemented `lineColor()`, `lineIcon()`, `statusColor()`, `shellBadge()` in lineFormatting.ts
- All return values match spec tables exactly
- Files changed: src/utils/helpers.ts, src/utils/lineFormatting.ts
- **Learnings for future iterations:**
  - Utility stubs were `export {}` — replaced entirely
  - lineFormatting.ts imports both `T` from theme.ts and types from types.ts
  - shellBadge bg values use raw rgba strings, not T token references (spec-specific values)
  - Switch statements on union types provide exhaustiveness checking without a default case
---

## 2026-03-15 23:40 - US-006
- Implemented `MOCK_SESSIONS` (4 sessions) and `SPAWNER_TEMPLATES` (4 templates) in mockSessions.ts
- api-server: bash/active/8 lines, git-workflow: zsh/active/8 lines, docker-build: bash/idle/6 lines, test-runner: bash/error/5 lines
- All line types, statuses, shell types, and content match spec exactly
- Files changed: src/data/mockSessions.ts
- **Learnings for future iterations:**
  - mockSessions.ts was a stub (`export {}`) — replaced entirely
  - Multi-line stdout in the spec (e.g., docker build output) should be combined into a single SessionLine with `\n` to match line counts
  - The "On mount, 4 panels" criterion belongs to the hook (US-007), not this data story
  - Mock data imports types from `../types` — keep type imports consistent
---

## 2026-03-15 23:40 - US-007
- Implemented full `useCanvasInteractions` hook in hooks/useCanvasInteractions.ts
- State: panels, activeId, dragging, resizing, panning, canvasOffset, zoom, showSpawner, maxZ ref
- Actions: bringToFront, closePanel, tilePanels, spawnSession, resetView
- Handlers: startDrag, startResize, startPan, handleWheel, setShowSpawner
- Initializes 4 panels from MOCK_SESSIONS with cascading positions (x=40+i*60, y=40+i*50)
- Window-level mousemove/mouseup in useEffect with proper cleanup
- Files changed: src/hooks/useCanvasInteractions.ts
- **Learnings for future iterations:**
  - The hook stub was `export {}` — replaced entirely
  - Hook contains both state management (US-007) and interaction logic (US-008/US-009) since they're all in one file
  - `startDrag` reads panels inside `setPanels` to get current panel position for offset calc (avoids stale closure)
  - `tilePanels` uses canvasOffset to position panels relative to current view, not absolute
  - React.MouseEvent types for handler params, native MouseEvent for window listeners
---

## 2026-03-15 23:44 - US-008
- Wired up Canvas.tsx with useCanvasInteractions hook — 3-level transform nesting, dot grid, pointer events
- Implemented SessionPanel.tsx with drag (title bar), resize (corner handle), close (red dot), bringToFront
- Updated Toolbar, SpawnerModal, KeyboardHints stubs to accept props from Canvas
- Updated App.tsx to render Canvas component (aliased as AgentCanvas)
- Updated App.test.tsx to check for 'api-server' panel text instead of removed 'Agent Canvas' div
- Cursor shows 'grabbing' during canvas pan
- Files changed: App.tsx, App.test.tsx, Canvas.tsx, SessionPanel.tsx, Toolbar.tsx, SpawnerModal.tsx, KeyboardHints.tsx
- **Learnings for future iterations:**
  - When Canvas wires up child components, their stubs must accept the props Canvas passes — use `void` for unused params
  - SessionPanel props: panel, isActive, onDragStart, onResizeStart, onClose, onBringToFront
  - Toolbar props: panelCount, zoom, onNewSession, onTile, onResetView
  - SpawnerModal props: onSpawn, onClose
  - The smoke test checks for rendered panel content — update if rendering changes
  - Terminal body and full line rendering are placeholders — full implementation in US-012/US-013
---

## 2026-03-15 23:47 - US-009
- Verified all 5 panel actions already implemented in useCanvasInteractions hook (from US-007/US-008)
- bringToFront: increments maxZ ref, updates panel z, sets activeId — wired to SessionPanel onBringToFront
- closePanel: filters panel array, clears activeId if match — wired to red traffic light onClick
- tilePanels: grid layout with cols=ceil(sqrt(n)), tileW=500, tileH=360, gap=20 — wired to Toolbar onTile
- spawnSession: creates panel with uid(), random position offset by canvasOffset, 2 initial lines, closes spawner — wired to SpawnerModal onSpawn
- resetView: sets canvasOffset to (0,0) and zoom to 1 — wired to Toolbar onResetView
- All quality checks pass: typecheck, lint, test
- Files changed: .ralph/prd.json (mark US-009 passes: true)
- **Learnings for future iterations:**
  - US-009 actions were already fully implemented in the hook during US-007 — this story was essentially a verification pass
  - All actions are wired through Canvas.tsx to child components via props
  - Toolbar and SpawnerModal are still stubs — they accept props but render empty divs until their own stories (US-011, US-015)
---

## 2026-03-15 23:50 - US-010
- Verified Canvas.tsx already implements all acceptance criteria (done during US-008 wiring)
- Canvas fills viewport (100vw x 100vh, overflow: hidden, background: T.bg)
- Dot grid uses radial-gradient with T.gridDot, spacing 24*zoom px, moves with pan/zoom
- 3-level transform nesting: canvas-area → zoom-layer (scale) → pan-layer (translate) → panels
- zoom-layer and pan-layer carry data-canvas="true" for click-through
- All quality checks pass: typecheck, lint, test
- Files changed: .ralph/prd.json (mark US-010 passes: true)
- **Learnings for future iterations:**
  - US-010 was already fully implemented during US-008 — Canvas.tsx was built complete when wiring up pointer interactions
  - The canvas-area div receives both mouseDown (pan) and onWheel (zoom) events
  - Dot grid backgroundPosition uses canvasOffset * zoom to track with panning
---

## 2026-03-15 23:52 - US-011
- Implemented full Toolbar component with logo, divider, 3 buttons, spacer, and status text
- Added .toolbar-btn and .toolbar-btn.primary CSS classes to global.css
- Logo: 26x26 gradient square with >_ character + "Agent Canvas" label
- Status text: "{N} session(s) · {zoom}%" with JetBrains Mono font
- All buttons wired to props: onNewSession, onTile, onResetView
- Files changed: src/components/Toolbar.tsx, src/styles/global.css
- **Learnings for future iterations:**
  - toolbar-btn CSS classes use hardcoded hex values matching T tokens (CSS can't reference JS const)
  - The primary button variant uses rgba for background/border, not T tokens
  - global.css will accumulate more classes in US-017 (scrollbar, animations, spawner-option)
---

## 2026-03-15 23:55 - US-012
- Added status dot (● character with statusColor), shell badge (from shellBadge()), and CWD label to title bar
- Restructured title bar: traffic lights → session info wrapper (status dot + name + shell badge) → CWD label
- Imported statusColor and shellBadge from lineFormatting utils
- Files changed: src/components/SessionPanel.tsx
- **Learnings for future iterations:**
  - SessionPanel already had outer container, traffic lights, and resize handle from US-008 — US-012 only needed title bar metadata additions
  - Shell badge uses an IIFE `(() => { const badge = shellBadge(...); return ... })()` to avoid separate variable declaration
  - CWD label goes outside the session info wrapper as a separate flex item with flexShrink: 0
  - Terminal body is still a placeholder — full line rendering is US-013
---

## 2026-03-15 23:57 - US-013
- Replaced terminal body placeholder with full line rendering
- Each line renders with icon column (lineIcon) and text column, styled per line type
- stdin: purple accent color, fontWeight 500
- stdout: default text color
- stderr: red left-border (2px) + tinted red background
- system: italic, dimmed color
- Active sessions show pulsing purple dot (uses pulse animation from global.css) with "Working..." text
- Auto-scrolls to bottom on mount/line changes via useRef + useEffect
- Files changed: src/components/SessionPanel.tsx
- **Learnings for future iterations:**
  - Terminal body uses JetBrains Mono at 12.5px with lineHeight 1.6
  - The pulse animation must exist in global.css for the active session indicator to work (added in US-017)
  - lineColor and lineIcon utilities handle all per-type styling — just pass line.t
  - Spread syntax with conditional objects `...(condition ? {styles} : {})` keeps inline styles clean
---

## 2026-03-15 23:58 - US-014
- Verified resize handle already fully implemented from US-008
- Resize handle at bottom-right (position: absolute, right: 0, bottom: 0) with SVG grip lines (3 diagonal lines)
- Dragging resizes panel: width clamped [320, 1200], height clamped [200, 900] via clamp() in useEffect handler
- startResize calls bringToFront(id) to bring panel to front on resize
- All quality checks pass: typecheck, lint, test
- Files changed: .ralph/prd.json (mark US-014 passes: true)
- **Learnings for future iterations:**
  - US-014 was already fully implemented during US-008 — SessionPanel had the resize handle + SVG from the start
  - Resize logic lives in useCanvasInteractions hook's useEffect mousemove handler
  - Several stories (US-009, US-010, US-014) ended up as verification passes because US-007/US-008 built everything at once
---

## 2026-03-16 00:21 - US-001 (TDD reset re-verification)
- Verified project scaffold is complete with all 14 prescribed source files
- Only react/react-dom as runtime dependencies (React 19.2.4)
- Typecheck, lint, and 1 test all pass
- No code changes needed — scaffold was already committed in prior iterations
- Files changed: .ralph/prd.json (mark US-001 passes: true)
- **Learnings for future iterations:**
  - PRD was reset (all passes: false) with TDD requirements added — stories need re-verification
  - The @/ import alias and __tests__/ folder were configured in the reset commit
  - Many stories (US-001 through US-014) were already implemented but need TDD tests written per new criteria
---

## 2026-03-16 00:22 - US-002
- Verified external font loading already implemented from prior iteration
- Google Fonts @import loads DM Sans + JetBrains Mono (400/500/600/700) with display=swap
- Body font-family set to "DM Sans" for UI chrome
- Terminal body, status text, and toolbar status use JetBrains Mono via inline styles
- global.css imported in main.tsx
- All quality checks pass: typecheck, lint, test
- Files changed: .ralph/prd.json (mark US-002 passes: true)
- **Learnings for future iterations:**
  - US-002 was already implemented — just needed verification and PRD update
  - Many pre-TDD-reset stories are already implemented; check existing code before writing new code
---

## 2026-03-16 00:24 - US-003
- Wrote TDD tests for T token object in src/__tests__/theme.test.ts
- Tests verify: T is exported, has exactly 19 keys, all keys present, each key maps to correct value
- Implementation already existed from prior iteration — all 22 tests passed immediately
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/theme.test.ts (new)
- **Learnings for future iterations:**
  - theme.ts was already implemented pre-TDD-reset; only tests needed to be added
  - Test pattern: describe block for object, nested describe for values, one it() per key/value assertion
  - Use `Object.keys(T).sort()` for order-independent key comparison
---

## 2026-03-16 00:25 - US-004
- Verified all TypeScript interfaces and type aliases already implemented in types.ts
- Exports: LineType, SessionLine, SessionStatus, ShellType, Session, PanelState, DragState, ResizeState, PanState, SpawnerTemplate, ShellBadgeStyle
- All types match spec exactly
- Quality gate: typecheck, lint, test all pass (23 tests)
- Files changed: .ralph/prd.json (mark US-004 passes: true)
- **Learnings for future iterations:**
  - types.ts was already implemented from pre-TDD-reset iterations — only needed verification
  - No TDD criteria for this story, so no test file needed
---

## 2026-03-16 00:26 - US-005
- Wrote TDD tests for helpers.ts (clamp boundary tests, uid format/uniqueness) and lineFormatting.ts (lineColor, lineIcon, statusColor, shellBadge per type)
- Implementation already existed from prior iteration — all tests passed immediately
- Quality gate: typecheck, lint, test all pass (46 tests)
- Files changed: src/__tests__/helpers.test.ts (new), src/__tests__/lineFormatting.test.ts (new)
- **Learnings for future iterations:**
  - helpers.test.ts: 8 tests (5 clamp boundary, 3 uid checks)
  - lineFormatting.test.ts: 15 tests (4 lineColor, 4 lineIcon, 3 statusColor, 4 shellBadge)
  - shellBadge tests assert raw rgba strings for bg, T tokens for fg, and string labels
  - statusColor "idle" falls through to default case returning T.textDim
---

## 2026-03-16 00:27 - US-006
- Wrote TDD tests for MOCK_SESSIONS and SPAWNER_TEMPLATES in src/__tests__/mockSessions.test.ts
- Tests verify: array lengths, each session's name/shell/status/line-count, template labels, template shell/desc fields
- Implementation already existed from prior iteration — all 9 new tests passed immediately (55 total)
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/mockSessions.test.ts (new)
- **Learnings for future iterations:**
  - mockSessions.test.ts: 9 tests (5 MOCK_SESSIONS, 1 field check, 3 SPAWNER_TEMPLATES)
  - Line counts: api-server=8, git-workflow=8, docker-build=6, test-runner=5
  - The "On mount, 4 panels" criterion belongs to the hook (US-007), not this data story
---

## 2026-03-16 00:29 - US-007
- Wrote TDD tests for useCanvasInteractions hook in src/__tests__/useCanvasInteractions.test.ts
- Tests verify: initial panels length 4, activeId null, zoom 1, canvasOffset {0,0}, showSpawner false
- Tests verify: cascading positions (x=40+i*60, y=40+i*50), dimensions 520x380, incrementing z-index
- Tests verify: all 10 action/handler functions returned, panning state returned
- Implementation already existed from prior iteration — all 21 new tests passed immediately (76 total)
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/useCanvasInteractions.test.ts (new)
- **Learnings for future iterations:**
  - renderHook from @testing-library/react used for hook testing (no wrapper needed)
  - Hook returns 10 functions + 5 state values — test all return types
  - The hook uses MOCK_SESSIONS for init, so panel count matches mock data length (4)
---

## 2026-03-16 00:31 - US-008
- Extended src/__tests__/useCanvasInteractions.test.ts with 15 new tests for pointer interactions
- handleWheel tests: zoom +/- 0.05 with ctrlKey/metaKey, clamp to [0.3, 2.0], no-op without modifier, preventDefault called
- startDrag tests: stopPropagation, sets activeId, brings to front (z-index update)
- startResize tests: stopPropagation, sets activeId, brings to front
- startPan tests: clears activeId on canvas background click, guard prevents pan on non-canvas elements
- Implementation already existed from prior iterations — all 91 tests pass (15 new + 76 existing)
- Files changed: src/__tests__/useCanvasInteractions.test.ts
- **Learnings for future iterations:**
  - Use `as unknown as React.WheelEvent` / `React.MouseEvent` to cast mock event objects in tests
  - handleWheel needs both ctrlKey and metaKey tested (Mac vs Windows)
  - startPan guard checks `target !== currentTarget && !target.dataset.canvas` — test both paths
  - Use `vi.fn()` for stopPropagation/preventDefault in mock events
  - `act()` from @testing-library/react wraps state-changing hook calls
---

## 2026-03-16 00:33 - US-009
- Extended src/__tests__/useCanvasInteractions.test.ts with 16 new TDD tests for panel actions
- bringToFront: updates z-index to highest, sets activeId, supports switching active panel
- closePanel: removes panel, clears activeId if matched, preserves activeId if different panel closed, idempotent for non-existent IDs
- tilePanels: arranges in grid (cols=ceil(sqrt(n)), tileW=500, tileH=360, gap=20), correct column count with fewer panels
- spawnSession: adds panel, 2 initial lines (system + stdin), closes spawner modal, sets new panel as active, assigns correct shell/cwd
- resetView: resets canvasOffset to {0,0} and zoom to 1
- Implementation already existed from prior iterations — all 107 tests pass (16 new + 91 existing)
- Files changed: src/__tests__/useCanvasInteractions.test.ts
- **Learnings for future iterations:**
  - When closing multiple panels in a single act(), capture IDs before calling closePanel — result.current won't update mid-act
  - SpawnerTemplate import from @/types needed for spawnSession test type safety
  - tilePanels offsets by canvasOffset — tests work at default offset (0,0) so grid positions are straightforward
---

## 2026-03-16 00:35 - US-010
- Wrote TDD tests for Canvas component in src/__tests__/Canvas.test.tsx
- Tests verify: canvas-area renders, zoom-layer has data-canvas, pan-layer has data-canvas, 3-level nesting with correct transforms, 4 session panels rendered on mount
- Implementation already existed from US-008 — all 5 new tests passed immediately (112 total)
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/Canvas.test.tsx (new)
- **Learnings for future iterations:**
  - Canvas has 4-level nesting: viewport > canvas-area (dot grid) > zoom-layer (scale) > pan-layer (translate) > panels
  - Navigate DOM via container.firstElementChild chains to reach nested layers
  - Zoom-layer transform contains "scale", pan-layer contains "translate" — assert with toContain
  - Test session panel presence by checking for session name text (api-server, git-workflow, etc.)
---

## 2026-03-16 00:36 - US-011
- Wrote TDD tests for Toolbar component in src/__tests__/Toolbar.test.tsx
- Tests verify: Agent Canvas logo text, >_ icon, 3 buttons (+ New Session, Tile, Reset View), status text with session count and zoom %, click handlers
- Implementation already existed from prior iteration — all 10 new tests passed immediately (122 total)
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/Toolbar.test.tsx (new)
- **Learnings for future iterations:**
  - Toolbar props: panelCount, zoom, onNewSession, onTile, onResetView
  - Status text format: "{N} session(s) · {zoom}%" — use Math.round(zoom * 100)
  - Tile and Reset View buttons have emoji prefixes (⊞, ⌂) — use regex matchers (/Tile/, /Reset View/) in tests
  - Use vi.fn() for handler props and assert toHaveBeenCalledTimes(1) after fireEvent.click
---

## 2026-03-16 00:39 - US-012
- Wrote TDD tests for SessionPanel component in src/__tests__/SessionPanel.test.tsx
- Tests verify: session name, 3 traffic light dots, red dot click calls onClose, status dot, shell badge, CWD path, clicking panel calls onBringToFront, active/inactive border styling
- 9 new tests, all pass (131 total)
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/SessionPanel.test.tsx (new)
- **Learnings for future iterations:**
  - jsdom converts hex colors to rgb in style properties — use rgb values in assertions (e.g., `rgb(45, 53, 72)` not `#2d3548`)
  - Traffic lights are 3 divs with `border-radius: 50%` — query with `querySelectorAll('div[style*="border-radius: 50%"]')`
  - SessionPanel props: panel, isActive, onDragStart, onResizeStart, onClose, onBringToFront
  - onClose is called with panel id, not event — assert `toHaveBeenCalledWith('panel-id')`
  - Remove unused imports (e.g., T) to avoid lint errors — tests use rgb strings directly
---

## 2026-03-16 00:42 - US-013
- Extended src/__tests__/SessionPanel.test.tsx with 7 new TDD tests for terminal output rendering
- Tests verify: all session lines render, correct line count in terminal body, correct icon characters (·, $, │, !), stdin purple accent color + fontWeight 500, stderr red left-border styling, active session shows "Working..." text, idle session hides "Working..." text
- Implementation already existed from prior iteration — all 138 tests pass (7 new + 131 existing)
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/SessionPanel.test.tsx
- **Learnings for future iterations:**
  - Don't destructure `{ container }` from renderPanel() if only using `screen` queries — lint will flag unused vars
  - Access terminal body as `panel.children[1]` (children[0] is title bar)
  - Active session has 4 line divs + 1 working indicator div = 5 children in terminal body
  - stdinText.style.color in jsdom returns rgb format — use `rgb(192, 132, 252)` for T.accent (#c084fc)
---

## 2026-03-16 00:43 - US-014
- Added 3 TDD tests to SessionPanel.test.tsx for resize handle
- Tests verify: resize handle renders (nwse-resize cursor), contains SVG, mouseDown calls onResizeStart with panel id
- Implementation already existed from US-008 — all 141 tests pass (3 new + 138 existing)
- Quality gate: typecheck, lint, test all pass
- Files changed: src/__tests__/SessionPanel.test.tsx
- **Learnings for future iterations:**
  - Query resize handle via `div[style*="nwse-resize"]` CSS selector
  - onResizeStart is called with (id, event) — use `expect.any(Object)` for the event arg
  - SVG is nested inside the resize handle div — query with `.querySelector('svg')`
---

## 2026-03-16 00:46 - US-015
- Wrote 10 TDD tests for SpawnerModal: heading, subtext, 4 template cards, shell badges, template labels/descriptions, onSpawn callback, backdrop onClose, stopPropagation on modal card
- Implemented full SpawnerModal component: backdrop with T.overlay, modal card with fadeIn, 4 template cards with staggered slideUp, shell badges via shellBadge()
- Added .spawner-option and .spawner-option:hover CSS classes to global.css
- Files changed: src/__tests__/SpawnerModal.test.tsx (new), src/components/SpawnerModal.tsx, src/styles/global.css
- **Learnings for future iterations:**
  - SpawnerModal imports SPAWNER_TEMPLATES directly — it doesn't receive templates via props
  - Backdrop onClick calls onClose; modal card uses e.stopPropagation() to prevent bubble
  - .spawner-option CSS class uses hardcoded hex matching T tokens (same pattern as .toolbar-btn)
  - Shell badges appear 3x bash + 1x zsh across the 4 templates — useful for assertion counts
  - Remove unused type imports to avoid lint errors (SpawnerTemplate not needed in tests)
---

## 2026-03-16 00:48 - US-016
- Wrote 4 TDD tests for KeyboardHints: renders pan, zoom, move, and resize hint text
- Implemented KeyboardHints component: absolute bottom 12px, centered, zIndex 9999, JetBrains Mono 10px, T.textMuted
- Hints separated by · dots with opacity 0.4
- pointerEvents: none and userSelect: none to avoid interfering with canvas interactions
- Files changed: src/__tests__/KeyboardHints.test.tsx (new), src/components/KeyboardHints.tsx
- **Learnings for future iterations:**
  - KeyboardHints takes no props — it's rendered directly by Canvas with no data dependency
  - Use pointerEvents: none on overlay UI so it doesn't block canvas mouse events
  - Component uses translateX(-50%) with left: 50% for centering
---

## 2026-03-16 00:49 - US-017
- Added 3 keyframe animations (pulse, fadeIn, slideUp) and custom scrollbar styling to global.css
- Global reset, .toolbar-btn, .toolbar-btn.primary, and .spawner-option classes already existed from prior iterations
- Files changed: src/styles/global.css
- **Learnings for future iterations:**
  - Most of US-017 was already implemented incrementally during US-002, US-011, and US-015
  - Only keyframe animations and scrollbar styling were missing
  - Prettier reformats condensed CSS (single-line keyframes) into multi-line — commit includes prettier formatting
---
